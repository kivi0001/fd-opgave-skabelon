---
import AccordionItem from "../components/AccordionItem.astro";

const { data, exclusive } = Astro.props;
---

<section class="accordion-container">
  <h3>FAQ</h3>
  <h2>Customer Questions</h2>

  <div>
    {data.map((item) => <AccordionItem {...item} exclusive={exclusive} />)}
  </div>
</section>

<style>
  @layer components {
    section {
      padding-block: 5rem;
      background: #f5f5f5;
      border-radius: var(--border-card);

      margin-bottom: var(--space-xl);
      grid-column: 2/4;
    }
    div {
      padding: 1rlh;
      max-width: 725px;
      margin-inline: auto;
    }
    h2,
    h3 {
      display: flex;
      justify-content: center;
      text-align: center;
    }
    h3 {
      color: var(--color-text-muted);
      font-size: var(--p-size);
    }
    /* section {
      padding: 3rem 1rem;
      background: #f5f5f5;
      border-radius: var(--border-card);
    }

    div {
      padding: 0;
      max-width: 725px;
      margin-inline: auto;
    }

    h2,
    h3 {
      display: flex;
      justify-content: center;
      text-align: center;
    }

    h3 {
      color: var(--color-text-muted);
      font-size: var(--p-size);
      margin-bottom: 0.5rem;
    }

    h2 {
      margin-bottom: 2rem;
    } */

    /* 📱 Mobil tweaks */
    /* @media (max-width: 900px) {
      section {
        padding: 2rem 1rem;
         margin: 0;
      }
       div {
      padding: 0:;
      max-width: 725px;
      margin-inline: auto;
    }

      h2 {
        font-size: 1.6rem;
      }

      h3 {
        font-size: 0.9rem;
      }
    }*/
  }
</style>

<!-- <style>
  section {
    padding-block: 3rem;
    background: #f5f5f5;
    border-radius: var(--border-card);
  }
  div {
    padding: 1rlh;
    max-width: 725px;
    margin-inline: auto;
  }
  h2,
  h3 {
    display: flex;
    justify-content: center;
    text-align: center;
  }
  h3 {
    color: var(--color-text-muted);
    font-size: var(--p-size);
  }
</style> -->
